#!/bin/bash
# 启动开发服务器（带动态配置刷新）
# 本机会监听 0.0.0.0:5173，局域网内其他设备可通过 http://<你的IP>:5173 访问

cd "$(dirname "$0")"
echo "🚀 启动 Paper Digest 开发服务器..."
echo "📍 本地访问: http://localhost:5173"
echo "📡 局域网访问: http://$(ifconfig en0 2>/dev/null | awk '/inet / {print $2}' || hostname -I | awk '{print $1}'):5173"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
npx vitepress dev docs --host 0.0.0.0 --port 5173
