# Paper Digest - ArXiv Agent Memory 论文速览

每日 ArXiv Agent Memory 领域论文自动整理站点。

---

## 🚀 快速开始

### 1. 本地开发

```bash
# 方式1: 使用脚本
./dev.sh

# 方式2: 直接使用 npm
npm run docs:dev
```

启动后访问：
- **本机**: http://localhost:5173
- **局域网**: http://你的IP:5173 （同一 WiFi 下的设备可访问）

### 2. 构建站点

```bash
./build.sh
# 或
npm run docs:build
```

构建输出到 `docs/.vitepress/dist/` 目录。

### 3. 预览构建结果

```bash
./preview.sh
# 或
npm run docs:preview
```

---

## 📡 分享给别人查看的 3 种方式

### 方式一：局域网分享（最简单，仅限同一网络）

启动开发服务器后，在同一 WiFi/局域网内的设备可以直接访问：

```bash
./dev.sh
```

控制台会显示局域网地址，例如：
```
📡 局域网访问: http://192.168.1.100:5173
```

把 `http://192.168.1.100:5173` 发给别人即可（需在同一网络）。

---

### 方式二：GitHub Pages（推荐，免费公网访问）

#### 步骤 1: 创建 GitHub 仓库

```bash
# 在 GitHub 创建新仓库，然后推送代码
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/你的用户名/paper-digest.git
git push -u origin main
```

#### 步骤 2: 配置 GitHub Pages

1. 打开仓库 → Settings → Pages
2. Source 选择 "GitHub Actions"

#### 步骤 3: 触发部署

GitHub Actions 会自动部署，或者手动触发：
- 仓库 → Actions → Deploy to GitHub Pages → Run workflow

#### 访问地址

部署成功后，访问：
```
https://你的用户名.github.io/paper-digest
```

#### 自动更新

- 每天北京时间 08:00 自动重新构建
- push 代码到 main 分支自动重新构建
- 可手动触发 Actions 重新构建

---

### 方式三：Vercel / Netlify（推荐，国内访问更快）

#### Vercel 部署

1. 访问 [vercel.com](https://vercel.com) 并用 GitHub 账号登录
2. Import Project → 选择 paper-digest 仓库
3. 配置：
   - Framework Preset: VitePress
   - Build Command: `npm run docs:build`
   - Output Directory: `docs/.vitepress/dist`
4. Deploy

#### Netlify 部署

1. 访问 [netlify.com](https://netlify.com)
2. Add new site → Import from Git
3. 选择仓库，配置同上
4. Deploy

---

## 📝 动态配置说明

### 自动文件列表

配置文件 `docs/.vitepress/generateConfig.mjs` 会自动：

1. **扫描** `/Users/eamonliang/paper-digest/docs/` 目录
2. **提取标题** 从每个 md 文件的第一个 `# ` 标题
3. **排序** 按文件修改时间倒序（最新的在前）
4. **生成导航**：
   - 顶部导航：显示最新的 5 个文件
   - 左侧边栏：显示所有文件

### 支持的文件名格式

| 文件名 | 显示名称 |
|--------|----------|
| `20260210_agent_memory.md` | 02-10 Agent Memory |
| `2026-02-08.md` | 02-08 |
| `my_research_paper.md` | My Research Paper |
| （带 `# 标题` 内容） | 提取的标题优先 |

### 添加新文件

只需把 `.md` 文件放入 `/Users/eamonliang/paper-digest/docs/`，刷新页面即可自动显示在导航中。

---

## 🔧 目录结构

```
vitepress/                          # VitePress 项目（配置+构建）
├── docs/
│   └── .vitepress/
│       ├── config.mjs              # 主配置（调用动态生成）
│       └── generateConfig.mjs      # 动态生成 nav/sidebar
├── .github/workflows/deploy.yml    # GitHub Pages 自动部署
├── dev.sh                          # 启动开发服务器
├── build.sh                        # 构建
├── preview.sh                      # 预览构建结果
└── package.json

paper-digest/                       # 实际文档内容（独立目录）
└── docs/
    ├── index.md                    # 首页
    ├── 20260208_agent_memory.md
    ├── 20260209_agent_research_weekly.md
    └── 20260210_agent_memory.md
```

---

## 📌 常见问题

### Q: 修改了 md 文件，页面没更新？

刷新浏览器即可。VitePress 会自动检测文件变化，但配置文件修改需要重启服务。

### Q: 想手动调整导航顺序？

编辑 `docs/.vitepress/generateConfig.mjs` 中的排序逻辑，或直接在 `config.mjs` 中硬编码 nav。

### Q: 局域网访问不了？

检查防火墙设置，确保允许 5173 端口。Mac 用户需在 "系统设置 → 网络 → 防火墙" 中允许。

### Q: GitHub Pages 部署失败？

检查：
1. 仓库 Settings → Pages → Source 设置为 "GitHub Actions"
2. 检查 Actions 日志排查错误
3. 确保 `package.json` 中的 scripts 配置正确

---

## 🎯 功能特性

- ✅ 动态文件列表（自动扫描 md 文件）
- ✅ 自动提取文档标题
- ✅ 按修改时间排序
- ✅ 本地局域网访问
- ✅ GitHub Pages 自动部署
- ✅ 站内搜索
- ✅ 响应式设计
- ✅ 代码高亮 + 行号
